name: Pull Request

on:
  pull_request

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        go-version: [1.13]
    steps:
      - uses: actions/checkout@v1
      #      - name: Push Docker Image to Github Registry
      #        uses: whoan/docker-build-with-cache-action@v5
      #        with:
      #          username: "${{ github.actor }}"
      #          password: "${{ secrets.GITHUB_TOKEN }}"
      #          image_name: ${{ secrets.flytegithub_repo }}/operator
      #          image_tag: ${{ github.sha }}
      #          push_git_tag: true
      #          registry: docker.pkg.github.com
      - uses: engineerd/setup-kind@v0.4.0
      - name: End2End
        env:
          DOCKER_USERNAME: ${{ github.actor }}
          DOCKER_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
        run: |
          kubectl cluster-info
          kubectl get pods -n kube-system
          echo "current-context:" $(kubectl config current-context)
          echo "environment-kubeconfig:" ${KUBECONFIG}
          PROPELLER=${{ secrets.flytegithub_repo }}/operator:5e302257c66f9056655a0d3fd9f34ed48fe37be8 make end2end_execute
#          PROPELLER=${{ secrets.flytegithub_repo }}/operator:${{ github.sha }} make end2end_execute
#      - name: Set up Go@${{ matrix.go-version }}
#        uses: actions/setup-go@v1
#        with:
#          go-version: ${{ matrix.go-version }}
#      - name: Run end-to-end tests
#        run: PROPELLER=${{ secrets.flytegithub_repo }}/operator:${{ github.sha }} make end2end
